[project]
name = "prostructure"
version = "0.1.0"
description = "ProStructure static server + cavity API"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "scipy>=1.11",
]

[tool.uv]
package = false
